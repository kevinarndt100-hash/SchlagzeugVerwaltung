<Window x:Class="SchlagzeugVerwaltung.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" Title="Schlagzeug Unterricht Verwaltung" Height="750" Width="1200" MinHeight="700" MinWidth="1000" Background="#0D1117" WindowStartupLocation="CenterScreen" KeyDown="Window_KeyDown">
  <Window.Resources>
    <!-- Modern Button Styles -->
    <Style x:Key="PrimaryButton" TargetType="Button">
      <Setter Property="Background" Value="#238636"/>
      <Setter Property="Foreground" Value="#FFFFFF"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,10"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="FontWeight" Value="500"/>
      <Setter Property="MinHeight" Value="36"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#2EA043"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#238636"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="DangerButton" TargetType="Button">
      <Setter Property="Background" Value="#DA3633"/>
      <Setter Property="Foreground" Value="#FFFFFF"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="16,10"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="FontWeight" Value="500"/>
      <Setter Property="MinHeight" Value="36"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F85149"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SecondaryButton" TargetType="Button">
      <Setter Property="Background" Value="#21262D"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="16,10"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="FontWeight" Value="500"/>
      <Setter Property="MinHeight" Value="36"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#30363D"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SidebarButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="#8B949E"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="12,10"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#21262D"/>
                <Setter Property="Foreground" Value="#C9D1D9"/>
              </Trigger>
              <Trigger Property="Tag" Value="Active">
                <Setter Property="Background" Value="#1F6FEB"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <!-- Modern Input Style -->
    <Style x:Key="ModernTextBox" TargetType="TextBox">
      <Setter Property="Background" Value="#0D1117"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="12,10"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="CaretBrush" Value="#C9D1D9"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
              <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="#58A6FF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <!-- Modern ComboBox Style -->
    <Style x:Key="ModernComboBox" TargetType="ComboBox">
      <Setter Property="Background" Value="#0D1117"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="12,10"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <ToggleButton x:Name="ToggleButton" BorderBrush="#30363D" BorderThickness="1" Background="#0D1117" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="ToggleButton">
                    <Border Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="4">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="24"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Column="0"/>
                        <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Fill="#8B949E" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                      </Grid>
                    </Border>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>
              <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Margin="12,10,28,10" VerticalAlignment="Center" HorizontalAlignment="Left"/>
              <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                  <Border x:Name="DropDownBorder" Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="4" Margin="0,2,0,0">
                    <Border.Effect>
                      <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                      <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                    </ScrollViewer>
                  </Border>
                </Grid>
              </Popup>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="ComboBoxItem">
      <Setter Property="Background" Value="#0D1117"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="Padding" Value="8,6"/>
      <Style.Triggers>
        <Trigger Property="IsHighlighted" Value="True">
          <Setter Property="Background" Value="#30363D"/>
        </Trigger>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#1F6FEB"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    
    <Style TargetType="ListBoxItem">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="Padding" Value="8,6"/>
    </Style>
    <Style TargetType="ScrollBar">
      <Setter Property="Background" Value="#0D1117"/>
    </Style>
    <Style TargetType="ContextMenu">
      <Setter Property="Background" Value="#161B22"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
    </Style>
    <Style TargetType="MenuItem">
      <Setter Property="Background" Value="#161B22"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="Padding" Value="8,6"/>
    </Style>
    <!-- Modern DataGrid Style -->
    <Style x:Key="ModernDataGrid" TargetType="DataGrid">
      <Setter Property="Background" Value="#0D1117"/>
      <Setter Property="Foreground" Value="#C9D1D9"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="RowBackground" Value="#0D1117"/>
      <Setter Property="AlternatingRowBackground" Value="#161B22"/>
      <Setter Property="GridLinesVisibility" Value="None"/>
      <Setter Property="HeadersVisibility" Value="Column"/>
      <Setter Property="SelectionMode" Value="Single"/>
      <Setter Property="SelectionUnit" Value="FullRow"/>
      <Setter Property="CanUserAddRows" Value="False"/>
      <Setter Property="CanUserDeleteRows" Value="False"/>
      <Setter Property="AutoGenerateColumns" Value="False"/>
      <Setter Property="RowHeight" Value="44"/>
    </Style>
    <Style x:Key="ModernDataGridColumnHeader" TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="#161B22"/>
      <Setter Property="Foreground" Value="#8B949E"/>
      <Setter Property="FontWeight" Value="600"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="12,10"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
      <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>
    <Style x:Key="ModernDataGridCell" TargetType="DataGridCell">
      <Setter Property="Padding" Value="12,0"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="DataGridCell">
            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter VerticalAlignment="Center"/>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#1F6FEB"/>
          <Setter Property="Foreground" Value="#FFFFFF"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="DataGridRow">
      <Setter Property="Background" Value="Transparent"/>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#21262D"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <!-- Card Style -->
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="#161B22"/>
      <Setter Property="BorderBrush" Value="#30363D"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="Padding" Value="24"/>
      <Setter Property="Margin" Value="0,0,0,16"/>
      <Setter Property="Effect">
        <Setter.Value>
          <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="2"/>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="240"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    
    <!-- Left Sidebar -->
    <Border Grid.Column="0" Background="#0D1117" BorderBrush="#30363D" BorderThickness="0,0,1,0">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Logo/Title -->
        <StackPanel Grid.Row="0" Margin="16,20,16,30">
          <TextBlock Text="Schlagzeug" FontSize="18" FontWeight="700" Foreground="#FFFFFF"/>
          <TextBlock Text="Unterricht" FontSize="18" FontWeight="700" Foreground="#FFFFFF"/>
        </StackPanel>
        
        <!-- Navigation -->
        <StackPanel Grid.Row="1" Margin="8,0">
          <Button x:Name="NavSchueler" Content="Schüler" Style="{StaticResource SidebarButton}" Click="NavSchueler_Click"/>
          <Button x:Name="NavTermine" Content="Termine" Style="{StaticResource SidebarButton}" Click="NavTermine_Click"/>
          <Button x:Name="NavMonat" Content="Monatsübersicht" Style="{StaticResource SidebarButton}" Click="NavMonat_Click"/>
          <Button x:Name="NavZahlungen" Content="Zahlungen" Style="{StaticResource SidebarButton}" Click="NavZahlungen_Click"/>
          <Button x:Name="NavWochennotizen" Content="Wochennotizen" Style="{StaticResource SidebarButton}" Click="NavWochennotizen_Click"/>
          <Button x:Name="NavDrum" Content="Drum Notation" Style="{StaticResource SidebarButton}" Click="NavDrum_Click"/>
        </StackPanel>
      </Grid>
    </Border>
    
    <!-- Main Content Area -->
    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Background="#0F141A">
      <Grid Margin="32">
        <!-- Schüler Page -->
        <Grid x:Name="PageSchueler" Visibility="Visible">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="Schüler" FontSize="28" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,24"/>
          
          <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="320"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Schüler Liste -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,16,0">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Alle Schüler" FontSize="16" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                
                <ListBox Grid.Row="1" Name="SchuelerListBox" Background="Transparent" BorderThickness="0" SelectionChanged="SchuelerListBox_SelectionChanged">
                  <ListBox.ContextMenu>
                    <ContextMenu>
                      <MenuItem Header="Neuer Termin" Click="ContextNeuerTermin_Click"/>
                      <MenuItem Header="Zahlung erfassen" Click="ContextZahlungErfassen_Click"/>
                      <MenuItem Header="Notiz hinzufügen" Click="ContextNotizHinzufuegen_Click"/>
                    </ContextMenu>
                  </ListBox.ContextMenu>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border Background="#21262D" CornerRadius="6" Padding="12" Margin="0,4">
                        <StackPanel>
                          <TextBlock FontSize="14" FontWeight="500" Foreground="#FFFFFF" Text="{Binding Name}"/>
                          <TextBlock FontSize="12" Foreground="#8B949E" Text="{Binding Stundensatz, StringFormat={}{0:0.00} € / Std.}"/>
                        </StackPanel>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ListBox>
                
                <Button Grid.Row="2" Content="+ Neuer Schüler" Style="{StaticResource PrimaryButton}" Margin="0,16,0,0" Click="NeuerSchueler_Click"/>
              </Grid>
            </Border>
            
            <!-- Schüler Details -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Name="SchuelerDetailsPanel" Visibility="Collapsed">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Schülerdetails" FontSize="20" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,24"/>
                
                <StackPanel Grid.Row="1" Margin="0,0,0,16">
                  <TextBlock Text="Name" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <TextBox Name="TxtName" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                
                <StackPanel Grid.Row="2" Margin="0,0,0,16">
                  <TextBlock Text="Telefon" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <TextBox Name="TxtTelefon" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                
                <StackPanel Grid.Row="3" Margin="0,0,0,16">
                  <TextBlock Text="E-Mail" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <TextBox Name="TxtEmail" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                
                <StackPanel Grid.Row="4" Margin="0,0,0,16">
                  <TextBlock Text="Stundensatz (€)" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <TextBox Name="TxtStundensatz" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                
                <StackPanel Grid.Row="5">
                  <TextBlock Text="Fester Termin" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*"/>
                      <ColumnDefinition Width="100"/>
                      <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0" Name="CmbFixerWochentag" Style="{StaticResource ModernComboBox}">
                      <ComboBoxItem Content="--" Tag="0"/>
                      <ComboBoxItem Content="Montag" Tag="1"/>
                      <ComboBoxItem Content="Dienstag" Tag="2"/>
                      <ComboBoxItem Content="Mittwoch" Tag="3"/>
                      <ComboBoxItem Content="Donnerstag" Tag="4"/>
                      <ComboBoxItem Content="Freitag" Tag="5"/>
                      <ComboBoxItem Content="Samstag" Tag="6"/>
                      <ComboBoxItem Content="Sonntag" Tag="7"/>
                    </ComboBox>
                    <TextBox Grid.Column="2" Name="TxtFixeUhrzeit" Style="{StaticResource ModernTextBox}" ToolTip="z.B. 16:00"/>
                  </Grid>
                </StackPanel>
                
                <StackPanel Grid.Row="6" Margin="0,16,0,0">
                  <TextBlock Text="Notizen" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <TextBox Name="TxtNotizen" Style="{StaticResource ModernTextBox}" AcceptsReturn="True" TextWrapping="Wrap" Height="80" VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
                
                <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                  <Button Content="Löschen" Style="{StaticResource DangerButton}" Margin="0,0,12,0" Click="SchuelerLoeschen_Click"/>
                  <Button Content="Speichern" Style="{StaticResource PrimaryButton}" Click="SchuelerSpeichern_Click"/>
                </StackPanel>
              </Grid>
            </Border>
          </Grid>
        </Grid>
        
        <!-- Termine Page -->
        <Grid x:Name="PageTermine" Visibility="Collapsed">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="Termine" FontSize="28" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,24"/>
          
          <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="280"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,16,0">
              <StackPanel>
                <TextBlock Text="Neuer Termin" FontSize="16" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                <TextBlock Text="Datum" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <DatePicker Name="DatePickerTermin" SelectedDateChanged="DatePickerTermin_SelectedDateChanged" Margin="0,0,0,12"/>
                <TextBlock Text="Uhrzeit" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <TextBox Name="TxtTerminZeit" Style="{StaticResource ModernTextBox}" ToolTip="z.B. 16:00" Margin="0,0,0,16"/>
                <Button Content="Termin hinzufügen" Style="{StaticResource PrimaryButton}" Click="TerminHinzufuegen_Click"/>
              </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Style="{StaticResource Card}">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                  <TextBlock Text="Filter:" Foreground="#8B949E" FontSize="14" VerticalAlignment="Center" Margin="0,0,12,0"/>
                  <ComboBox Name="CmbTerminMonat" Width="130" Style="{StaticResource ModernComboBox}" SelectionChanged="CmbTerminMonat_SelectionChanged"/>
                  <ComboBox Name="CmbTerminJahr" Width="100" Style="{StaticResource ModernComboBox}" Margin="8,0,0,0" SelectionChanged="CmbTerminMonat_SelectionChanged"/>
                  <TextBlock Text="Schüler:" Foreground="#8B949E" FontSize="14" VerticalAlignment="Center" Margin="24,0,12,0"/>
                  <ComboBox Name="CmbTerminSchueler" Width="150" Style="{StaticResource ModernComboBox}" SelectionChanged="CmbTerminSchueler_SelectionChanged"/>
                </StackPanel>
                
                <DataGrid Grid.Row="1" Name="TermineDataGrid" Style="{StaticResource ModernDataGrid}" MouseDoubleClick="TermineDataGrid_MouseDoubleClick">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Datum" Width="150" Binding="{Binding Datum, StringFormat={}{0:dd.MM.yyyy HH:mm}}" ElementStyle="{StaticResource {x:Type TextBlock}}" Foreground="#C9D1D9"/>
                    <DataGridTextColumn Header="Schüler" Width="*" Binding="{Binding SchuelerName}" Foreground="#C9D1D9"/>
                    <DataGridTemplateColumn Header="Bezahlt" Width="80">
                      <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                          <CheckBox HorizontalAlignment="Center" IsChecked="{Binding Bezahlt, Mode=OneWay}" Click="TerminBezahlt_Click"/>
                        </DataTemplate>
                      </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="" Width="50">
                      <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                          <Button Content="✕" Background="#DA3633" Foreground="#FFFFFF" BorderThickness="0" Padding="8,4" Cursor="Hand" Click="TerminLoeschen_Click"/>
                        </DataTemplate>
                      </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                  </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </Border>
          </Grid>
        </Grid>
        
        <!-- Monatsübersicht Page -->
        <Grid x:Name="PageMonat" Visibility="Collapsed">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          
          <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24">
            <TextBlock Text="Monatsübersicht" FontSize="28" FontWeight="600" Foreground="#FFFFFF" VerticalAlignment="Center"/>
            <ComboBox Name="CmbMonat" Width="130" Style="{StaticResource ModernComboBox}" Margin="24,0,0,0" SelectionChanged="CmbMonat_SelectionChanged"/>
            <ComboBox Name="CmbMonatJahr" Width="100" Style="{StaticResource ModernComboBox}" Margin="8,0,0,0" SelectionChanged="CmbMonat_SelectionChanged"/>
            <Button Content="Export CSV" Style="{StaticResource SecondaryButton}" Margin="24,0,0,0" Click="ExportCSV_Click"/>
          </StackPanel>
          
          <Border Grid.Row="1" Style="{StaticResource Card}">
            <DataGrid Name="MonatsuebersichtDataGrid" Style="{StaticResource ModernDataGrid}">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Schüler" Width="*" Binding="{Binding SchuelerName}"/>
                <DataGridTextColumn Header="Stunden" Width="80" Binding="{Binding Stunden}"/>
                <DataGridTextColumn Header="Zu zahlen" Width="120" Binding="{Binding Bezahlt, StringFormat={}{0:0.00} €}"/>
                <DataGridTextColumn Header="Guthaben" Width="120" Binding="{Binding Guthaben, StringFormat={}{0:0.00} €}"/>
                <DataGridTextColumn Header="Ausstehend" Width="120" Binding="{Binding Ausstehend, StringFormat={}{0:0.00} €}"/>
              </DataGrid.Columns>
            </DataGrid>
          </Border>
          
          <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
              <TextBlock Text="Gesamteinnahmen: " FontSize="16" Foreground="#8B949E"/>
              <TextBlock Name="TxtGesamteinnahmen" FontSize="16" FontWeight="600" Foreground="#3FB950"/>
            </StackPanel>
          </Border>
        </Grid>
        
        <!-- Zahlungen Page -->
        <Grid x:Name="PageZahlungen" Visibility="Collapsed">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="Zahlungen" FontSize="28" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,24"/>
          
          <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="300"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,16,0">
              <StackPanel>
                <TextBlock Text="Zahlung erfassen" FontSize="16" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                
                <TextBlock Text="Schüler" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <ComboBox Name="CmbSchuelerZahlung" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16"/>
                
                <TextBlock Text="Betrag (€)" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <TextBox Name="TxtBetrag" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>
                
                <TextBlock Text="Datum" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <DatePicker Name="DatePickerZahlung" Margin="0,0,0,24"/>
                
                <Button Content="Zahlung speichern" Style="{StaticResource PrimaryButton}" Click="ZahlungSpeichern_Click"/>
                <Button Content="Alle zurücksetzen" Style="{StaticResource DangerButton}" Margin="0,12,0,0" Click="ZahlungenZuruecksetzen_Click"/>
              </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Style="{StaticResource Card}">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="Zahlungshistorie" FontSize="16" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                
                <DataGrid Grid.Row="1" Name="ZahlungenDataGrid" Style="{StaticResource ModernDataGrid}">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Datum" Width="100" Binding="{Binding Datum, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <DataGridTextColumn Header="Schüler" Width="*" Binding="{Binding SchuelerName}"/>
                    <DataGridTextColumn Header="Betrag" Width="120" Binding="{Binding Betrag, StringFormat={}{0:0.00} €}"/>
                    <DataGridTemplateColumn Header="" Width="50">
                      <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                          <Button Content="✕" Background="#DA3633" Foreground="#FFFFFF" BorderThickness="0" Padding="8,4" Cursor="Hand" Click="ZahlungLoeschen_Click"/>
                        </DataTemplate>
                      </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                  </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </Border>
          </Grid>
        </Grid>
        
        <!-- Wochennotizen Page -->
        <Grid x:Name="PageWochennotizen" Visibility="Collapsed">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="Wochennotizen" FontSize="28" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,24"/>
          
          <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="280"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,16,0">
              <StackPanel>
                <TextBlock Text="Filter" FontSize="16" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                
                <TextBlock Text="Schüler" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <ComboBox Name="CmbSchuelerWochennotiz" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16" SelectionChanged="CmbSchuelerWochennotiz_SelectionChanged"/>
                
                <TextBlock Text="Woche" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                <DatePicker Name="DatePickerWochennotiz" SelectedDateChanged="DatePickerWochennotiz_SelectedDateChanged"/>
              </StackPanel>
            </Border>
            
            <Border Grid.Column="1" Style="{StaticResource Card}">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Name="TxtAktuelleWoche" Text="Woche vom..." FontSize="16" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,16"/>
                
                <DataGrid Grid.Row="1" Name="WochennotizenDataGrid" Style="{StaticResource ModernDataGrid}">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Woche" Width="120" Binding="{Binding Woche, StringFormat={}{0:dd.MM.yyyy}}"/>
                    <DataGridTextColumn Header="Notiz" Width="*" Binding="{Binding Notiz}"/>
                    <DataGridTemplateColumn Header="" Width="50">
                      <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                          <Button Content="✕" Background="#DA3633" Foreground="#FFFFFF" BorderThickness="0" Padding="8,4" Cursor="Hand" Click="WochennotizLoeschen_Click"/>
                        </DataTemplate>
                      </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                  </DataGrid.Columns>
                </DataGrid>
                
                <StackPanel Grid.Row="2" Margin="0,16,0,0">
                  <TextBlock Text="Neue Notiz" Foreground="#8B949E" FontSize="12" Margin="0,0,0,6"/>
                  <TextBox Name="TxtNeueWochennotiz" Style="{StaticResource ModernTextBox}" AcceptsReturn="True" TextWrapping="Wrap" Height="80" Margin="0,0,0,12"/>
                  <Button Content="Notiz speichern" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" Click="WochennotizSpeichern_Click"/>
                </StackPanel>
              </Grid>
            </Border>
          </Grid>
        </Grid>
        
        <!-- Drum Notation Page -->
        <Grid x:Name="PageDrum" Visibility="Collapsed">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="Drum Notation" FontSize="28" FontWeight="600" Foreground="#FFFFFF" Margin="0,0,0,24"/>
          
          <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              
              <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock Text="GrooveScribe" FontSize="16" FontWeight="600" Foreground="#FFFFFF" VerticalAlignment="Center"/>
                <Button Content="Neu laden" Style="{StaticResource SecondaryButton}" Margin="16,0,0,0" Padding="12,6" Click="GrooveScribeReload_Click"/>
                <Button Content="Im Browser öffnen" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0" Padding="12,6" Click="GrooveScribeOpenInBrowser_Click"/>
              </StackPanel>
              
              <wv2:WebView2 Grid.Row="1" Name="GrooveScribeWebView"/>
            </Grid>
          </Border>
          
          <!-- Hidden placeholder controls for code-behind -->
          <TextBox Name="TxtDrumName" Visibility="Collapsed"/>
          <ListBox Name="DrumNotationListBox" Visibility="Collapsed"/>
          <Canvas Name="DrumStaffCanvas" Visibility="Collapsed"/>
          <TextBlock Name="SelectedNoteValueText" Visibility="Collapsed"/>
        </Grid>
      </Grid>
    </ScrollViewer>
  </Grid>
</Window>