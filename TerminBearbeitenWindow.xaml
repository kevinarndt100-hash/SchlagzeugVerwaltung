<Window x:Class="SchlagzeugVerwaltung.TerminBearbeitenWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Termin bearbeiten" Height="250" Width="350" WindowStartupLocation="CenterOwner" Background="#2D2D2D">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" Margin="0,0,0,15">
      <TextBlock Text="Datum:" Foreground="#FFFFFFFF" Margin="0,0,0,5" />
      <DatePicker Name="DatePickerTermin" />
    </StackPanel>
    <StackPanel Grid.Row="1" Margin="0,0,0,15">
      <TextBlock Text="Uhrzeit:" Foreground="#FFFFFFFF" Margin="0,0,0,5" />
      <TextBox Name="TxtZeit" ToolTip="z.B. 16:00" />
    </StackPanel>
    <CheckBox Name="ChkBezahlt" Grid.Row="2" Content="Bezahlt" Foreground="#FFFFFFFF" />
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
      <Button Content="Abbrechen" Padding="15,8" Margin="0,0,10,0" Click="Abbrechen_Click" />
      <Button Content="Speichern" Padding="15,8" Click="Speichern_Click" />
    </StackPanel>
  </Grid>
</Window>